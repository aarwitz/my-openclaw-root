# OpenClaw repo policy: deny by default, then allow a small human-authored set.

# Ignore everything unless explicitly allowed.
*

# Always keep ignore policy itself.
!.gitignore

# Root docs and core non-secret config snapshots.
!ARCHITECTURE.md
!CLAUDE.md
!SYSTEM_ARCHITECTURE.md
!TELEGRAM_EXECUTION_GUIDE.md
!cli_guide.md
!telegram_documentation.html.txt
!openclaw.json
!openclaw.json.last-good
!exec-approvals.json
!gateway-instance-id
!referenced_files.txt
!referenced_ids.txt

# Allow scripts folder; still ignore generated/script-local junk.
!scripts/
!scripts/**
scripts/__pycache__/
scripts/*.pyc
scripts/*.pyo
scripts/*.pyd

# Allow selected source/config helper folders.
!acpx/
!acpx/**
acpx/codex-home/
acpx/*.log

!completions/
!completions/**

!plugin-skills/
!plugin-skills/**

!workspace/
!workspace/**

!workspaces/
!workspaces/**

# iOS app and mobile backend source
!ios\ App/
!ios\ App/**
# never commit Robinhood session tokens or APNs device token files
ios\ App/backend/*session*.json
ios\ App/backend/*token*.json

# Keep editor/local runtime state out.
.vscode/

# Hard exclusions for sensitive/runtime data even if parent folders are unignored later.
credentials/
logs/
tmp/
media/
memory/
tasks/
delivery-queue/
locks/
flows/
nodes/
devices/
identity/
telegram/
cron/runs/
cron/jobs-state.json
agents/
npm/
canvas/
plugins/
qqbot/
workspaces/**/.tm-restore/

# Generic secret and transient patterns.
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd
**/*token*
**/*secret*
**/*password*
**/*apikey*
**/*api-key*
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
*.db-shm
*.db-wal
*.pid
*.lock
*.tmp
*.swp
*.swo
