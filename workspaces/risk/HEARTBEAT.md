# Keep this file empty (or with only comments) to skip heartbeat API calls.

# Risk is driven by the overseer pipeline (cron desk-sequence), not heartbeat.
# Add tasks below only if you want the agent to check something periodically.
