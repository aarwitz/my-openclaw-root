# Connectors package: deterministic, stdlib-only data fetchers for the trading
# pipeline. Each connector raises ConnectorError on irrecoverable failure so the
# classifier / executor fail loudly instead of silently degrading.
